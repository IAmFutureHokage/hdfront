import { OpenedDirective } from './opened.directive';

describe('OpenedDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenedDirective();
    expect(directive).toBeTruthy();
  });
});
